def is_happy(ticket_number    half_len = len(ticket_number) // 2
    first_half = ticket_number[:half_len]
    second_half = ticket_number[half_len:]
    sum(int(digit) for digit in first_half) == sum(int(digit) for in second_half)

print(is_happy_ticket('123123'))
